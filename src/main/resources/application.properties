spring.application.name=arvore-geneologia-pro

spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/arvore}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Gur1j0$e2026!}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

server.port=${PORT:8080}
server.address=0.0.0.0

jwt.secret=${JWT_SECRET:genealogia_local_dev_secret_1234567890_123456}
jwt.expiration=${JWT_EXPIRATION:86400000}

